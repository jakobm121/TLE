name: TLE 04b Audit Ratings

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  audit-ratings:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Audit ratings
        run: |
          python -m tle_machine.audit_ratings

      - name: Commit rating audit outputs
        if: always()
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add data/reports/ratings/rating_integrity_audit.json || true
          git add data/reports/ratings/rating_integrity_issues.csv || true
          git add data/reports/ratings/rating_integrity_warnings.csv || true

          if git diff --cached --quiet; then
            echo "No rating audit changes to commit."
          else
            git commit -m "Audit ratings integrity"
            git push
          fi
